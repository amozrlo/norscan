{
  "name": "norscan",
  "version": "1.0.0",
  "private": true,
  "description": "NOR ALNIBRAS offline carton scanner — QR + Arabic/English label OCR to inventory Excel",
  "scripts": {
    "bundle": "node scripts/bundle.mjs",
    "sync": "npm run bundle && cap sync android",
    "open": "cap open android",
    "apk": "npm run sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@zxing/library": "^0.21.3",
    "tesseract.js": "^5.1.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
